<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN" "http://www.ibatis.com/dtd/sql-map-2.dtd">

<sqlMap namespace="ReferCategorySpace">
	<typeAlias  alias="egovMap" type="egovframework.rte.psl.dataaccess.util.EgovMap"/>
	<typeAlias  alias="searchVO" type="egovframework.example.sample.service.SampleDefaultVO"/>
	<typeAlias  alias="referCategoryVO" type="ezen.service.vo.ReferCategoryVO"/>
	
<!-- REFER_CATEGORY -->
<insert id="referCategoryDAO.insertREFER_CATEGORY">
<!-- 	INSERT INTO REFER_CATEGORY
				(
				RC_ID,
				RC_STEP1,
				RC_STEP2,
				RC_STEP3,
				RC_NAME,
				RC_REGDATE
				 )
	values(
			) -->
	</insert>
	
	<update id="referCategoryDAO.updateREFER_CATEGORY">
<!-- 		UPDATE REFER_CATEGORY 
		SET  
		WHERE  -->
	</update>


	<delete  id="referCategoryDAO.deleteREFER_CATEGORY">
<!-- 		DELETE FROM REFER_CATEGORY 
		WHERE -->
	</delete>
	

	
	<select id="referCategoryDAO.selectREFER_CATEGORY" resultClass="egovMap">
<!-- 		SELECT 	
				RC_ID,
				RC_STEP1,
				RC_STEP2,
				RC_STEP3,
				RC_NAME,
				RC_REGDATE
		FROM 
				REFER_CATEGORY
		WHERE -->
	</select>

	<select id="referCategoryDAO.selectREFER_CATEGORY_Detail" resultClass="egovMap">
<!-- 		SELECT 	
				RC_ID,
				RC_STEP1,
				RC_STEP2,
				RC_STEP3,
				RC_NAME,
				RC_REGDATE
		FROM 
				REFER_CATEGORY
		WHERE -->
	</select>
	
	<select id="referCategoryDAO.selectREFER_CATEGORY_Count" resultClass="egovMap">
<!-- 		SELECT
				COUNT(
						RC_ID,
						RC_STEP1,
						RC_STEP2,
						RC_STEP3,
						RC_NAME,
						RC_REGDATE
					)
		FROM
				REFER_CATEGORY -->
	</select>
</sqlMap>

